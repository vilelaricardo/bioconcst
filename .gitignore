/target/
/experiment/
/results/
*.xlsx

# Local IDE settings (points at a JDK path specific to this machine) - never shared config.
/.vscode/

# CoverageInst's own compile/instrument/execution scratch space (see
# CoverageInstRun) - regenerated fresh on every run, never an artifact to keep.
/cov-experiment-*/
/cov-replay-*/
/pcfg-viz-work-*/
/pcfg-demo-*/

# ValiPar is an internal/unreleased tool - never publish it in this repo.
/docker/valipar/vendor/

# Reference papers for the LLM experiment - copyrighted PDFs, local reading only.
/papers/

# Internal research-strategy/debug notes (informal, not meant for the
# public repo tied to a published paper) - kept locally, never tracked.
/research_questions.md
/debug-geracao-quorum-handshake.md

# Session-local experiment-orchestration scripts (same convention as the
# other run-*.sh scripts already never tracked in this repo) - kept
# locally for reproducing a specific run, not part of the tracked tool.
/generate_williams_schedule.py
/run-interleaved-replication.sh
